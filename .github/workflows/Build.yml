name: UABEA Build-apk

on:
  push:
    branches:
      - android-port
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET 8
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Install Android Workload
        run: dotnet workload install android

      - name: Restore Android
        run: dotnet restore UABEAvalonia.Android/UABEAvalonia.Android.csproj

      - name: Build Android
        run: dotnet build UABEAvalonia.Android/UABEAvalonia.Android.csproj -c Release --no-restore

      - name: Upload Build Output
        uses: actions/upload-artifact@v4
        with:
          name: build-output
          path: |
            **/bin/Release/**
